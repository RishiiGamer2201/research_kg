# S2DN Data

In the live WSL workspace, `SDN/data` points to the GraIL inductive benchmark data used by S2DN.

The symlink itself is not included in this GitHub snapshot because Windows Git cannot index the WSL
link reliably. The benchmark data can be restored from the GraIL repository or from the copied
Windows-side `external/grail/data` folder.

Expected live WSL path:

`/home/admin_wsl/research_kg/S2DN/SDN/data`
